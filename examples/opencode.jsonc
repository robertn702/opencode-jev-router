{
  "$schema": "https://opencode.ai/config.json",
  "provider": {
    "jev-router": {
      "npm": "@ai-sdk/openai",
      "name": "Jev adaptive Astra",
      "options": {
        // With UPSTREAM_AUTH=bearer, set CLIPROXY_KEY to a local placeholder.
        // The router uses UPSTREAM_API_KEY from its own environment upstream.
        "apiKey": "{env:CLIPROXY_KEY}",
        "baseURL": "http://127.0.0.1:4320/v1"
      },
      "models": {
        "gpt-6-astra": {
          "name": "GPT-6 Astra with adaptive effort",
          "reasoning": true,
          "options": {
            "useResponses": true
          }
        }
      }
    }
  }
}
